# Generated web resources

Milah's frontend production files are generated in `frontend/dist/` and
embedded under the Qt resource prefix `/` by `CMakeLists.txt`. No generated
bundle is checked into this directory.
